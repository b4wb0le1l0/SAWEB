spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/studs
    username: s329464
    password: 7rEqb73PsV6VM0bn
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
